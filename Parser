import mysql.connector
import data as d
class sql():
    def __init__(self):
        pass
    def connect(self):
        self.mycon = con.connect(host = 'Localhost', user = 'root',password = 'HariOm@123', database = 'flight')
        self.mycursor = self.mycon.cursor()
    '''def fetch_countries():
        self.connect()
        qry = 'select country from Countries'
        self.mycursor.excecute(qry)
        data = self.mycursor.fetchall()
        self.mycon.close()
        return data'''
    def fetch_countries(self):
        self.countries = []
        for i in range(1,len(d.country) + 1):
            self.countries.append(d.country[i])
        return self.countries
